{
  "name": "grav-plugin-maw-builder",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Visual block builder for Grav Admin2. Builds admin-next/fields/blocks.js.",
  "scripts": {
    "build": "vite build && node scripts/finalize.mjs",
    "watch": "vite build --watch",
    "test": "node --test \"tests/js/*.test.mjs\""
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.28.0",
    "vite": "^6.3.0"
  }
}
