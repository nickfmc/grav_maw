{% embed 'blocks/_section.html.twig' with {default_background: 'accent', default_width: 'narrow'} %}
    {% block inner %}
        {% import 'macros/ui.html.twig' as ui %}
        <div class="cta">
            {% if block.eyebrow %}<span class="eyebrow"{{ maw_edit('eyebrow') }}>{{ block.eyebrow }}</span>{% endif %}
            {% if block.heading %}<h2 class="cta__title"{{ maw_edit('heading') }}>{{ block.heading }}</h2>{% endif %}
            {% if block.text %}<div class="lead cta__text"{{ maw_edit_md('text') }}>{{ block.text|markdown|raw }}</div>{% endif %}
            {{ ui.buttons(block.buttons, 'lg') }}
        </div>
    {% endblock %}
{% endembed %}
